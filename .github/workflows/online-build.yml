name: Legacy Direct Booking Online Build 015

on:
  workflow_dispatch:

jobs:
  legacy-build-015:
    runs-on: windows-latest
    steps:
      - name: Legacy notice
        shell: pwsh
        run: |
          Write-Host "Build 015 is retained as the proven prototype/reference."
          Write-Host "The active product direction is Direct Booking Web V1."
